export async function saveSubscription(
  subscriptionId: string,
  customerId: string,
) {
  // busca o usuario no banco do faunadb
  console.log(subscriptionId, customerId);
  console.log("ronaldo aqui");

  // salvar os dados  da subscription no faundb
}
